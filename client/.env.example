# Base URL of the Node/Express backend (deployed to Render).
# In local dev this points at the locally running server.
# In Vercel, set this in Project Settings -> Environment Variables.
REACT_APP_API_URL=http://localhost:5000
